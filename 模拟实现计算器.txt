#define _CRT_SECURE_NO_WARNINGS 1
#include<stdio.h>
#define  Num  5
#include<string.h>
int Add(int x,int y)
{
	return x + y;
}
int Sub(int x, int y)
{
	return x - y;
}
int Mul(int x,int y)
{
	return x * y;
}
int Div(int x, int y)
{
	return x / y;
}
void menu()
{
	printf("*******************************************\n");
	printf("*********** 1.Add  ****** 2.Sub ***********\n");
	printf("*********** 3.Mul  ****** 4.Div ***********\n");
	printf("***************** 0.Exit ******************\n");
	printf("*******************************************\n");
}

void Calc(int (*ptr)(int,int))
{
	int x = 0;
	int y = 0;
	printf("请输入俩个操作数，用空格隔开：");
	scanf("%d%d", &x, &y);
	printf("%d\n", ptr(x, y));
}
int main()
{
	int input = 0;
	do
	{
		menu();
		printf("请选择：");
		scanf("%d", &input);
		switch (input)
		{
		case 0:
			printf("退出程序！");
			break;
		case 1:
			Calc(Add);
			break;
		case 2:
			Calc(Sub);
			break;
		case 3:
			Calc(Mul);
			break;
		case 4:
			Calc(Div);
			break;
		default:
			printf("错误输入，请重新输入：");
			break;
		}
	} while (input);
	return 0;
}

int main()
{
	int input = 0;
	int x = 0;
	int y = 0;
	int (*arr[Num])(int, int) = { 0,Add,Sub,Mul,Div };
	do
	{
		menu();
		printf("请选择：");
		scanf("%d", &input);
		if (input >= 1 && input <= 4)
		{
			printf("请输入俩个操作数，用空格隔开：");
			scanf("%d%d", &x, &y);
			int ret = arr[input](x, y);
			printf("%d\n", ret);
		}
		else if (input == 0)
			printf("退出程序！");
		else
			printf("错误输入！请重新选择：");
	} while (input);
	/*do
	{
		menu();
		printf("请选择：");
		scanf("%d", &input);
		switch (input)
		{
		case 0:
			printf("退出程序！");
			break;
		case 1:
			printf("请输入俩个操作数，用空格隔开：");
			scanf("%d%d", &x, &y);
			printf("%d\n", Add(x, y));
			break;
		case 2:
			printf("请输入俩个操作数，用空格隔开：");
			scanf("%d%d", &x, &y);
			printf("%d\n", Sub(x, y));
			break;
		case 3:
			printf("请输入俩个操作数，用空格隔开：");
			scanf("%d%d", &x, &y);
			printf("%d\n", Mul(x, y));
			break;
		case 4:
			printf("请输入俩个操作数，用空格隔开：");
			scanf("%d%d", &x, &y);
			printf("%d\n", Div(x, y));
			break;
		default:
			printf("错误的输入，请重新选择：");
		}
	} while (input);*/
	return 0;
}